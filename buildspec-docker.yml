version: 0.2
phases:
    pre_build:
        commands:
            - echo Logging in to Amazon ECR...
            - $(aws ecr get-login --no-include-email --region sa-east-1)
    build:
        commands:
            - docker build -t expressapi:latest .
            - docker tag expressapi:latest 543454195198.dkr.ecr.sa-east-1.amazonaws.com/turma905:latest
    post_build:
        commands:
            - docker push 543454195198.dkr.ecr.sa-east-1.amazonaws.com/turma905:latest
